// it is just a supporting class library with one interface for example of LateBinding Inerface

namespace CarLibrary
{
    public interface ICar
    {
        void Acceleration();
        void Driver(string name, int age);
    }
}
